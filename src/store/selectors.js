/**
 * @param {object} state state
 * @param {string} name name
 * @returns {number|undefined} state
 */
// eslint-disable-next-line no-magic-numbers
export const getIndex = (state, name) => state.index[name];
